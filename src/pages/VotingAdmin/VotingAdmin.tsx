import React from 'react';
// import styles from './votingadmin.module.scss';
import Layout from '../../loyout/Layout';

const VotingAdmin = ():JSX.Element => (
  <Layout>
    <div>
      Voting_admin
    </div>
  </Layout>
);

export default VotingAdmin;
